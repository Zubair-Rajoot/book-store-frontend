import { BorrowDurationPipe } from './borrow-duration.pipe';

describe('BorrowDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new BorrowDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
